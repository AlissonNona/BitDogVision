#include "voice.h"
#include <stdio.h>

void voice_init() {
    // Placeholder para inicialização futura
}

void voice_announce(const char *message) {
    printf("[Voz] %s\n", message); // Implementação básica via serial
}